# FreeCyto backend — runtime dependencies.
#
# Dev/test-only packages live in requirements-dev.txt.

fastapi>=0.110.0
uvicorn[standard]>=0.28.0

# Numerics. Event matrices are float32 memmaps; see services/storage.py.
numpy>=1.24.0

# FCS I/O. Parsing is hand-rolled in services/fcs_parser.py; flowio is used for
# the FCS 3.1 export path and flowutils for compensation helpers.
flowio>=1.2.0
flowutils>=0.8.0

# ── Not currently imported ───────────────────────────────────────────────────
# Verified unused as of 2026-08-26 (no reference anywhere under backend/).
# They were pinned early for planned analysis features that were never built,
# and together pull in a very large transitive tree (numba, h5py, joblib, ...)
# that slowed every CI run and clean install. Uncomment when actually adopted.
#
# pandas>=2.0.0          # tabular export — models/table_models.py builds CSV directly
# scipy>=1.11.0          # density/contours are implemented on numpy in services/
# scikit-learn>=1.3.0    # reserved for clustering
# umap-learn>=0.5.0      # reserved for dimensionality reduction
# anndata>=0.10.0        # reserved for interop
# fcsparser>=0.2.0       # superseded by services/fcs_parser.py
